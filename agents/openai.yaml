interface:
  display_name: "Deep Debug"
  short_description: "Fresh full-scope bug audit with CRC reverify"
  default_prompt: "Use $deep-debug and set this as a goal to audit this code path from scratch for real bugs across domain correctness, implementation correctness, memory/resource behavior, and performance where applicable; verify each finding, fix confirmed issues, and CRC-reverify until no confirmed bugs remain."
